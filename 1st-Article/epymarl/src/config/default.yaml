# --- Defaults ---

# --- pymarl options ---
runner: "episode" # Runs 1 env for an episode
mac: "basic_mac" # Basic controller
env: "sc2" # Environment name
env_args: {} # Arguments for the environment
batch_size_run: 1 # Number of environments to run in parallel
test_nepisode: 100 # Number of episodes to test for # initially 20
test_interval: 20001 # Test after {} timesteps have passed # initially 2000
test_greedy: True # Use greedy evaluation (if False, will set epsilon floor to 0
log_interval: 20000 # Log summary of stats after every {} timesteps # initially 2000
runner_log_interval: 100000 # Log runner stats (not test stats) every {} timesteps # initially 2000
learner_log_interval: 100000 # Log training stats every {} timesteps # initially 2000
t_max: 20000 # Stop running after this many timesteps
use_cuda: True # Use gpu by default unless it isn't available
use_mps: True # Use Apple Silicon GPU (MPS) when CUDA is unavailable
buffer_cpu_only: True # If true we won't keep all of the replay buffer in vram

# --- Logging options ---
use_tensorboard: False # Log results to tensorboard
save_model: True # Save the models to disk
save_model_interval: 20000 # Save models every 20k timesteps (was 200k; more frequent to catch NaN divergence)
checkpoint_path: "" # Load a checkpoint from this path
evaluate: False # Evaluate model for test_nepisode episodes and quit (no training)
render: False # Render the environment when evaluating (only when evaluate == True)
load_step: 0 # Load model trained on this many timesteps (0 if choose max possible)
save_replay: False # Saving the replay of the model loaded from checkpoint_path
local_results_path: "results" # Path for local results

# --- RL hyperparameters ---
gamma: 0.99
batch_size: 32 # Number of episodes to train on. # initially 32
buffer_size: 15000 # Size of the replay buffer # initially 32. For 30000 timesteps, A good starting point is to set the replay buffer size at 10,000 to 50,000 experiences
lr: 0.0003 # Learning rate for agents (reduced from 0.0005 to prevent NaN divergence)
optim_alpha: 0.99 # RMSProp alpha
optim_eps: 0.00001 # RMSProp epsilon
grad_norm_clip: 10 # Reduce magnitude of gradients above this L2 norm # initially 10
add_value_last_step: True

# --- Agent parameters ---
agent: "rnn" # Default rnn agent
hidden_dim: 64 # Size of hidden state for default rnn agent. It is between 64 and 256
obs_agent_id: True # Include the agent's one_hot id in the observation
obs_last_action: True # Include the agent's last action (one_hot) in the observation

# --- Experiment running params ---
repeat_id: 1
label: "default_label"
hypergroup: null